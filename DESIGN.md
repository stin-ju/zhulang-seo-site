# DESIGN.md

## 气质与意象

把这个站想象成"奥运颁奖夜的深色演播厅 × 电竞战队信息板"——头顶是漆黑的穹顶，低饱和的午夜蓝铺满看台，球场草坪般的翠绿在屏幕边缘泛着微光，金色聚光灯打在领奖台正中央的第一名身上，照亮一面奖牌盘的轮廓。整体应给人"夜场颁奖、数据直播、严肃竞技"三层叠加的氛围，而不是科技 SaaS 仪表盘。

不是娱乐博彩站，也不是炫酷未来 UI；是认真做"AI 能力擂台"赛后复盘的演播室。

## 视觉策略

- 整体走"深色看台 + 数据卡牌"路线：大面积午夜蓝/沥青黑底，关键卡片用低层次的渐变玻璃质感（不是廉价玻璃拟态，而是接近冰球场围栏的哑光玻璃）。
- 强调对象（第一名、命中、关键比分）使用金色描边或翠绿色填充，其他保持低调。
- 卡片之间用极细的金色/翠绿色发丝边作为分隔，避免投资 SaaS 风的圆角白卡。
- 数据可视化语言要克制：大数字、强对比百分比、命中网格用色块即可，不需要折线图和雷达图。

## 配色方案

- `--bg-night` `#0B1220`：深夜看台主背景（午夜蓝偏黑）。
- `--bg-deep` `#0F1A2E`：次级容器、卡片底色（比主背景略亮，像看台座椅）。
- `--bg-elevated` `#16243D`：浮起卡片/表头，像被聚光灯轻扫过的座位。
- `--turf` `#10B981`：翠绿，借自球场草坪，承担"命中/正确"的语义。
- `--turf-soft` `rgba(16,185,129,0.15)`：命中格背景。
- `--gold` `#F5C242`：金色奖牌，承担"冠军/强调/排名第一"。
- `--gold-soft` `rgba(245,194,66,0.12)`：金色高亮卡片背景。
- `--miss` `#3F4A60`：未命中灰，低饱和不抢戏。
- `--text-primary` `#E8EEF7`：主文本。
- `--text-secondary` `#94A3B8`：辅助文本。
- `--divider` `rgba(255,255,255,0.08)`：分隔线。

## 字体排版

- 中文优先 "Noto Sans SC"，英文与数字优先 "Inter"，等宽数字（比分、百分比）用 "JBMono / Roboto Mono"，没有则降级到 `font-variant-numeric: tabular-nums`。
- 比赛比分、命中率百分比、排名数字使用大字号（48–72px）+ 金色或翠绿，字重 700+。
- 标题层级简洁，最多三级；正文 14–15px，行高放松到 1.6 让深色背景下不挤压。

## 动效与交互

- 默认安静，悬停才微微"亮灯"：卡片上 hover 时金色发丝边淡入、阴影加深 1 档，过渡 200ms，曲线 `cubic-bezier(0.22, 1, 0.36, 1)`（柔和减速）。
- 排行榜冠军卡进入时一次性的"金光扫过"shimmer 效果，仅播放一次，不循环。
- 命中网格的 ✓ 出现用 120ms 淡入即可，禁止弹跳/旋转等卡通动效。

## 页面结构

- 全站统一顶部导航：左侧站点 logo + 名称，右侧四个 Tab（排行榜 / 比赛 / AI / 关于）。
- 排行榜首页：聚光灯下的冠军大卡 + 其余 6 名梯队卡片网格；底部一行总比赛场次/已确认场次。
- 比赛列表：按时间倒序的卡片瀑布，左侧时间轴竖线 + 圆点节点；待比赛卡片去饱和、已确认卡片亮金色边框。
- 比赛详情：顶部宽幅"实际赛果"横幅；下方 7×6 命中矩阵，命中绿、未命中灰，最右一列总命中数用大字。
- AI 个人页：左侧 AI 头像/名称/总命中率大数字，右侧维度命中率四个进度条 + 下方 28 场的命中条带（每场一段，绿色块=命中，灰色块=未命中）。

## 设计禁忌

- 不要科技蓝渐变 + 蓝紫光晕 + 玻璃拟态那一套通用 AI 产品味。
- 不要使用任何与赌博、赔率、投注相关的视觉符号（骰子、扑克、轮盘、闪烁霓虹"WIN"字样）。
- 不要彩虹色、霓虹粉、纯黑 #000、纯白 #FFF。
- 不要给数据加无意义的折线/雷达图装饰；图表必须服务于"谁更准"这一核心问题。
- 不要圆角过大（>16px）的卡片，会显得幼态；统一 12px 圆角。
- 不要在整页背景叠加噪点纹理或星空粒子，干扰阅读。
